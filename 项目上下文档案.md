# 项目上下文档案：赛博桌宠 CyberPet

> 本档案基于代码库、PRD 文档、界面文案、Git 提交历史等**可观察证据**整理，服务于 PDF 简历与网页版简历。所有无法从证据确认的内容均标注「需要我补充」或「推测，需确认」。
>
> 证据基础：产品需求文档 `cyberpet_prd.docx`（v1.0，2026-04-09，含 40 条 EARS 需求拆解）、19 次 Git 提交（2026-04-09 至 2026-06-05，约 2 个月）、5 个窗口的完整产品界面、8 个提示词资产文件、多供应商 AI 接入配置体系。

---

## 1. 项目一句话定位

**版本 A（推荐用于简历）：**
这是一款面向知识工作者的 AI 桌面任务伴侣——用户只需把「今天要做的事」用一句话告诉桌宠，它就完成任务拆解、时间排程、到点提醒、超时干预和当日复盘的完整闭环。

**版本 B（突出差异化叙事，适合面试口述）：**
这不是又一个待办清单工具，而是把「AI 理解你的任务并陪你做完」做成桌面陪伴体验的产品——AMD 锐龙 AI 智能体创新应用大赛「工作学习」赛道参赛作品。（差异化定位直接来自 PRD 第九节「叙事验证」原文）

**版本 C（突出 AI 产品能力，适合 AI PM / FDE 岗位）：**
一个把三类 AI 能力（LLM 对话规划、AI 生图、AI 生视频）封装成普通人可直接使用的产品功能的桌面应用——用户全程不需要懂提示词、不需要懂模型，但可以自由切换 7+ 家云端/本地 AI 供应商。

> 推荐：投 AI 产品经理 / FDE 岗位用**版本 C 做主定位、版本 A 做补充**；版本 B 适合作为面试开场的故事钩子。

---

## 2. 项目解决的真实问题

### 表层问题（用户看到的功能需求）
- 每天的待办事项太多太乱，需要一个工具帮忙记录、排程、提醒。
- 想要一个好看、有陪伴感的桌面宠物，而不是冷冰冰的清单软件。

### 中层问题（用户为什么需要它）
- 传统 todo 工具只负责「记录」，但**拆解任务、估算耗时、安排顺序、监督执行**这些最累的事，全靠用户自己的意志力。
- PRD 原文佐证：核心价值主张是「**把今天要做的事告诉我，我帮你拆解、排布、盯着做完**」（PRD §1.1）——产品瞄准的是「记录之后没人管」这个空白。

### 深层问题（真正想降低什么成本）
- **降低「从想法到行动」的启动成本**：用户用自然语言说一句话，系统输出可执行的当日计划，省去手动建任务、填时间、排顺序的全部操作。
- **把自我管理从「意志力问题」变成「系统问题」**：到点提醒、超时 150% 主动干预、完成后鼓励、晚间复盘——执行环节的监督被系统接管。
- **降低对 AI 工具的信任成本**：AI 复盘被产品规则强制要求「点名真实任务、诚实提及未完成项、只给一条具体建议」（`src/prompts/dailyReflect.ts` 中的硬约束），避免 AI 输出空泛鸡汤导致用户弃用。
- 是否有真实用户反馈验证以上问题假设：**需要我补充**。

---

## 3. 目标用户与使用场景

### 目标用户（直接来自 PRD §1.2，证据明确）
| 用户层 | 描述 | 核心诉求 |
|---|---|---|
| 主要用户 | 知识工作者（产品经理、设计师、开发者、内容创作者） | 每天多个并行任务需要管理 |
| 次要用户 | 大学生及考研/考证备考人群 | 学习计划拆解和时间管理 |
| 边缘用户 | 自由职业者 / 一人公司 | 自我管理多角色任务 |

### 典型使用场景
1. **早晨规划**：上班/开始学习前，双击桌宠说「今天上午写竞品分析报告，下午 3 点开会，晚上准备演讲稿」，得到拆解好的可视化日程（此场景直接来自 PRD 第七节的 5 分钟演示流程设计）。
2. **执行中被陪伴**：到点时桌宠气泡提醒「该开始『XX』了」，给出「现在开始 / 稍后 5 分钟」两个按钮；某项任务超过预估时间 150% 时主动弹窗问「需要调整节奏吗？」。
3. **晚间复盘**：打开任务面板「足迹」页，点「陪我回顾今天」，AI 基于当天真实完成/未完成记录生成一段朋友口吻的复盘，配合打卡热力墙看长期坚持情况。
4. **个性化养成**：在「形象工作室」用一句中文描述（如「戴贝雷帽的柴犬艺术家」）生成专属桌宠形象和整套动作动画，或导入自己的图片/视频。

### 最能体现项目价值的用户路径
**「一句话 → 当日计划 → 执行陪伴 → 复盘」的完整闭环**——它证明这个产品不是功能堆叠，而是围绕「一天的任务生命周期」设计的任务路径。

---

## 4. 产品结构 / 功能地图

### 模块一：桌宠本体（产品的情感入口）
- **作用**：所有功能的唯一视觉触点。透明置顶小窗常驻桌面，有待机/思考/鼓励/休息等多种状态动画和 8 种随机小动作（伸懒腰、打哈欠等）。
- **用户可以完成**：双击打开对话、右键呼出菜单、通过气泡按钮响应提醒（看到了 / 稍后提醒 / 现在开始 / 已完成）。
- **对项目价值的贡献**：把「任务管理工具」的冷感包装成「伙伴在帮我」的温感，这是产品差异化的核心载体。
- **可用于简历的表达**：设计了以桌宠为唯一触点的非打断式提醒交互（气泡 + 按钮选择 + 超时自动顺延）。
- 适合写进简历：✅

### 模块二：自然语言任务规划（核心 AI 能力）
- **作用**：把用户的一句话变成结构化的当日计划。
- **用户可以完成**：用日常语言描述待办 → 自动识别意图（闲聊 / 新任务 / 改任务三类路由）→ 自动拆解为 2-7 个子任务并估时排程 → 继续用自然语言修改（「把会议挪到 4 点」「拆得更细一点」）。
- **对项目价值的贡献**：产品的核心差异化——「AI 理解任务」而非「定时提醒」。
- **可用于简历的表达**：设计了对话即规划的任务输入方式，将自然语言自动转化为结构化任务、时间排程和提醒。
- 适合写进简历：✅（最重要的模块）

### 模块三：任务面板（执行中枢，4 个页签）
- **作用**：日程（时间轴 + 冲突检测）、任务（卡片 + 子任务操作）、提醒（自定义定时提醒）、足迹（复盘）。
- **用户可以完成**：查看可视化日程、开始/完成/跳过/调整任务、管理独立提醒、回看历史。
- **对项目价值的贡献**：承接 AI 规划结果的「落地容器」，让 AI 输出变成可操作界面。
- **可用于简历的表达**：按「一天任务生命周期」组织信息架构（计划 → 执行 → 提醒 → 回顾），而非按功能罗列。
- 适合写进简历：✅（作为信息架构能力证据）

### 模块四：AI 当日复盘（「足迹」）
- **作用**：AI 基于当天真实任务数据生成朋友口吻的复盘，配合打卡热力墙、连续天数统计、完成轨迹清单。
- **用户可以完成**：一键生成/重新生成复盘、按 14/30/90/180 天查看坚持情况、回看已完成任务详情。
- **对项目价值的贡献**：补全闭环的最后一环，提供长期留存的情感价值（打卡墙点亮的成就感）。
- **可用于简历的表达**：为 AI 复盘设计了「必须点名真实任务、诚实提及未完成、只给一条具体建议」的输出约束，解决 AI 总结空泛不可信的问题。
- 适合写进简历：✅（AI 输出质量控制的代表性证据）

### 模块五：形象工作室（AI 创作流程的消费级封装）
- **作用**：5 步向导（灵感描述 → 基准图 → 动作清单 → 帧生成 → 预览确认），让不懂 AI 绘画的用户生成专属桌宠形象和整套动作动画；也支持本地导入图片/视频。
- **用户可以完成**：中文一句话描述角色 → 可选 AI 润色为专业生图提示词 → 4 张候选图选 1 → 勾选 7 个预设动作（待机/思考/鼓励等，每个动作绑定桌宠状态）→ 批量生成动画帧 → 可选为动作生成视频 → 保存即用。过程可随时暂停，草稿自动保留。
- **对项目价值的贡献**：证明「把专业 AIGC 工作流抽象成普通人能走完的向导」的产品能力——这是 AI 产品经理岗位最对口的证据。
- **可用于简历的表达**：把「生图提示词工程 + 多帧一致性 + 绿幕抠像」等专业环节封装成 5 步向导，用户全程零提示词知识即可产出可用的角色动画。
- 适合写进简历：✅✅（AI PM 岗位的招牌模块）

### 模块六：技能系统（用户可扩展的 AI 工作流）
- **作用**：用 `/命令` 触发可复用的 AI 工作流（类似把常用提示词做成快捷指令），且内置「技能创造器」——用户描述需求，AI 自动生成一个新技能并入库。
- **用户可以完成**：输入 `/` 看到自动补全、一键调用技能（如邮件润色）、用自然语言创建自定义技能。
- **对项目价值的贡献**：把「提示词复用」做成了产品功能，体现「AI 能力资产化」的意识。
- **可用于简历的表达**：设计了用户可自定义的 AI 技能体系，并实现「用 AI 创建 AI 技能」的元能力（含防递归、字段白名单等安全约束）。
- 适合写进简历：✅

### 模块七：AI 供应商配置中心（多供应商 + 成本管控）
- **作用**：统一管理对话模型、图像生成、视频生成三类 AI 能力的供应商配置，分「云端 API」和「本地部署」两组。
- **用户可以完成**：在 7+ 家供应商间切换（即梦/可灵/海螺/Vidu/Gemini/Replicate 云端 + ComfyUI/OpenAI 兼容本地）、查看每秒视频生成价格估算、设置每日生图配额防超支、一键检测服务可用性。
- **对项目价值的贡献**：体现对「AI 成本焦虑」这一真实用户问题的判断——本地免费通道 + 云端计价透明 + 配额熔断。
- **可用于简历的表达**：为多家 AI 供应商设计统一接入框架，配套成本透明化（按秒计价展示）与每日配额防护。
- 适合写进简历：✅（FDE / AI 集成方向的证据）

---

## 5. 核心用户流程

### 流程一：一句话生成当日计划（核心流程）
用户双击桌宠 → 对话面板打开 → 输入「今天上午写报告，下午 3 点开会，晚上备演讲稿」→ 系统自动判断这是「新任务」而非闲聊 → AI 拆解出主任务和子任务、估算耗时、排入时间轴 → 回复「已为你规划了 3 个任务，预计总耗时 2h30m……打开任务面板查看详细日程，或继续告诉我需要调整的地方~」→ 任务面板自动刷新出可视化日程。
**降低的成本**：从「逐条建任务、填时间、排顺序」的十几次操作，压缩为一句话。

### 流程二：执行陪伴闭环（最体现产品温度）
任务到点 → 桌宠气泡弹出「该开始『XX』了」+「现在开始 / 稍后 5 分钟」按钮 → 用户点「现在开始」则任务进入执行态，点「稍后」则 5 分钟后再次提醒 → 若执行超过预估时间的 150%，弹出「需要调整节奏吗？」并给「继续 / 完成 / 跳过 / 调整」四个选项 → 完成时桌宠播放鼓励动画。
**降低的成本**：提醒不是打断式弹窗轰炸，而是「给选择题」——3 分钟不响应自动顺延，不制造负罪感。

### 流程三：AI 当日复盘
打开任务面板「足迹」页 → 若当天有任务记录，看到「今天已经完成 X 项了，让我陪你回顾一下」→ 点「✨ 陪我回顾今天」→ AI 拿到当天真实的完成/未完成/跳过清单 → 生成 3-5 句朋友口吻的复盘（被规则强制点名具体任务、不许编数据）→ 下方是打卡热力墙和连续天数。
**降低的成本**：把「写日报/复盘」这件没人坚持的事变成一次点击。

### 流程四：AI 创建专属桌宠形象（5 步向导）
进入形象工作室 → 首屏明确告知「5 步引导：灵感描述 → 基准图 → 动作清单 → 帧生成 → 预览确认。过程中可随时暂停，草稿会保留」→ Step1 填角色名 + 中文一句话描述（给了示例「戴贝雷帽的柴犬艺术家」），可选 AI 润色 → Step2 生成 4 张候选图选 1，滑块实时调抠图效果 → Step3 勾选动作（每个动作标注触发场景，如「思考：AI 正在思考你的指令」）→ Step4 批量生成动画帧，可暂停、单帧重试 → Step5 预览动画、可选生成动作视频，保存后立即切换。
**降低的成本**：用户不需要懂提示词、绿幕、序列帧任何概念，每一步都有示例和实时预览。

### 流程五：用 AI 创建自定义技能
对话框输入需求（如「帮我做一个把口语草稿改成正式邮件的技能」）→ AI 生成完整技能定义（触发命令、名称、提示词模板）→ 自动校验入库 → 之后输入 `/` 即可在补全列表中看到并使用。
**降低的成本**：用户把重复性 AI 用法一次性固化成可复用指令，不必每次重写提示词。

---

## 6. 项目中的关键产品判断

### 判断一：把「任务管理」重新定义为「对话即规划」
- **解决的问题**：传统 todo 工具录入成本高，用户坚持不下来；真正的瓶颈不是记录而是拆解和排程。
- **证据**：PRD §1.1 核心价值主张原文；聊天意图三分类路由设计（闲聊/新任务/改任务，`src/prompts/chatRouter.ts`）；支持自然语言改任务（「拆得更细一点」对应 PRD DECOMP-004 需求条目）。
- **可用于简历的表达**：将产品核心交互从「表单录入」重构为「自然语言对话」，并为意图识别、任务拆解、任务修改分别设计了结构化输出规则。
- **适合放在**：PDF + 网页版（这是主叙事）。

### 判断二：用产品规则约束 AI 输出的可信度
- **解决的问题**：AI 生成的总结/复盘容易空泛、捏造，用户用一次就不信了。
- **证据**：复盘提示词中的三条硬约束（`src/prompts/dailyReflect.ts`）——必须出现真实任务名、未完成的必须点名但语气不责备、只给「一条」跟今天挂钩的建议，并明令「禁止编造列表外的任务或数字」。
- **可用于简历的表达**：为 AI 复盘功能制定「具体、诚实、向前」三原则输出约束，从产品规则层面解决 AI 输出不可信的问题。
- **适合放在**：PDF（一句话）+ 网页版（展开讲）。这是 AI PM 面试的高分素材。

### 判断三：把专业 AIGC 工作流抽象成 5 步消费级向导
- **解决的问题**：生成一套角色动画本来需要懂提示词、图像一致性、绿幕抠像、序列帧——普通用户完全无法操作。
- **证据**：5 步向导组件（`src/components/character/steps/`）；首屏文案明确管理预期（「过程中可随时暂停，草稿会保留」）；每步都有中文示例和实时预览；草稿中间态持久化设计（`character_drafts` 表 + 自动保存），生成中断可恢复。
- **可用于简历的表达**：把多模型协作的 AIGC 生产流程（文案润色 → 生图 → 多帧一致性 → 抠像 → 视频）拆解为 5 步向导，每步可暂停恢复，用户零专业知识可走完全程。
- **适合放在**：PDF + 网页版（配截图最有说服力）。

### 判断四：AI 成本透明化 + 配额熔断
- **解决的问题**：AI 生成功能「不知道要花多少钱」是用户不敢用的真实顾虑。
- **证据**：视频供应商目录中每家标注每秒人民币价格（`src/services/video/providerCatalog.ts`，如 Veo ¥2.7/s、本地 ComfyUI ¥0）；界面在启用视频时实时显示「单条 4s ≈ ¥X.XX」；每日生图配额超限前阻断并给出替代建议「请到设置中调整上限或改用 ComfyUI 本地」（`src/services/imageQuota.ts`）；抠图刻意放在前端本地完成并在界面注明「不再消耗 API 配额」。
- **可用于简历的表达**：为 AI 生成功能设计成本透明机制（按秒计价展示、每日配额熔断、免费本地通道兜底），把「AI 很贵」的焦虑转化为用户可控的决策。
- **适合放在**：PDF（强烈推荐，这是少见的「商业敏感度」证据）+ 网页版。

### 判断五：同一能力给两条路——云端快捷 vs 本地免费
- **解决的问题**：不同用户的付费意愿和动手能力差异巨大。
- **证据**：设置面板把供应商明确分为两组并配引导文案——「云端 API：按 Key 计费，提交任务即可开跑」/「本地部署：连接你自己电脑上启动的开源服务」（`src/services/providers/presets.ts` 中的 VENDOR_GROUPS）；对话模型同样支持 API 模式和本地 Ollama 模式切换。
- **可用于简历的表达**：为每类 AI 能力设计云端/本地双通道，让轻量用户即开即用、极客用户零成本自托管。
- **适合放在**：网页版为主，PDF 可并入判断四。

### 判断六：提醒交互的「非打断、可顺延、不施压」原则
- **解决的问题**：提醒类产品最大的流失原因是「提醒让人烦」。
- **证据**：所有提醒走桌宠气泡而非系统级弹窗；按钮永远给「台阶」（稍后 5 分钟 / 还没完成）；提醒 3 分钟无响应自动转为稍后提醒而非持续闪烁（PetWindow 中的 ACK 超时机制）；超时干预的文案是商量语气「需要调整节奏吗?」。
- **可用于简历的表达**：为执行提醒设计「非打断式」交互规范：气泡 + 选择题 + 自动顺延，监督而不施压。
- **适合放在**：网页版（交互设计细节），PDF 可一笔带过。

### 判断七：从「端侧推理」到「云端 + 本地双模式」的务实转向 ⚠️
- **解决的问题**：在参赛叙事（端侧 NPU 推理、数据不上云）和按期交付可用产品之间做取舍。
- **证据**：PRD（2026-04-09）规划的是端侧 LLM + AMD NPU 推理路径，且把「拆解质量不稳定」「集成复杂度」列为最高风险并预设了「通过本地 HTTP 服务解耦」的应对策略（PRD 第八节）；实际代码实现为云端 API 代理 + 本地 Ollama 兼容服务两种模式（`src/services/llm/`），未走 NPU 专用推理路径。
- **可用于简历的表达**：识别端侧推理的工程风险后，按 PRD 中预设的风险应对方案转向「云端 API + 本地服务」双模式，保证产品按期可用。⚠️ **转向的具体决策过程和时间点：需要我补充**（这是面试必被追问的点，建议提前准备好故事）。
- **适合放在**：网页版「复盘反思」节。**注意：简历和档案中不要把端侧 NPU 推理、数据完全本地化写成已实现能力**——代码与此不符，面试官打开仓库即可证伪。

---

## 7. 我的可能贡献与可验证证据

> 以下为「贡献候选」，供你逐条确认后写入简历。项目无其他署名贡献者（Git 历史仅一个作者 ch1109），**「独立完成（AI 协作）」的定位有 Git 证据支撑**，但建议你确认是否有他人参与（如美术素材、测试反馈）。

### 产品设计相关
- **贡献候选**：完整的产品信息架构设计——5 个窗口、任务面板 4 页签、形象工作室 5 步向导的结构划分。
  - 可观察证据：窗口路由结构（`src/main.tsx` 按窗口标签分发）、PRD 四层架构表。
  - 需要补充确认：结构是你先想好再让 AI 实现，还是 AI 提议你筛选？
  - 简历可用程度：**高**
- **贡献候选**：全套界面文案与引导语设计（示例驱动的输入提示、商量语气的超时干预、萌系空状态文案）。
  - 可观察证据：组件内大量精心设计的中文文案（如「今天还没有安排任务。双击我打开对话，告诉我你想做点什么吧～」）。
  - 需要补充确认：文案是你逐条打磨还是 AI 初稿后你筛改？
  - 简历可用程度：**中-高**

### 需求与业务抽象相关
- **贡献候选**：用 EARS 方法把模糊的产品想法拆解为 40 条带优先级（P0/P1/P2）、带触发条件的结构化需求，并明确 MVP 边界。
  - 可观察证据：PRD 第三、四、五节完整的 EARS 需求表和 MVP 范围界定。**你已确认 PRD 为你主导 + AI 协作完成**。
  - 需要补充确认：EARS 方法是你已掌握的方法论还是为此项目学习的？
  - 简历可用程度：**高**（这是 PM 岗位最对口的硬证据）
- **贡献候选**：把「桌宠陪伴」抽象为状态机（待机/思考/鼓励/休息/提醒/庆祝/撒娇等状态，动作与状态绑定），把「一天的任务」抽象为 计划→任务→子任务→提醒→复盘 的对象模型。
  - 可观察证据：桌宠状态类型定义、数据库表结构（daily_plans / tasks / subtasks / reminders / daily_reviews）。
  - 简历可用程度：**高**（复杂场景抽象能力的直接证据）

### AI 协作相关
- **贡献候选**：以 AI 协作方式独立完成全部设计与开发（vibe coding），并为 AI 助手建立项目记忆体系。
  - 可观察证据：① PRD 技术选型表中明确写出选 React 的理由是「生态成熟，**Claude Code 生成效率高**」——立项时就把 AI 协作效率纳入技术决策；② 仓库内存在为 AI 助手编写的项目说明文档 `CLAUDE.md` 和长期记忆文档 `.claude/PROJECT.md`（含踩坑记录），这是成熟 AI 协作工作流的直接痕迹；③ 首日 9 个提交完成 8 个阶段（Phase 1-8）的开发节奏，符合 AI 协作的高速交付特征。
  - 需要补充确认：使用的具体 AI 工具组合、典型的协作分工方式、印象深刻的纠偏案例。
  - 简历可用程度：**高**（但需要你补充过程细节才能经得起追问）
- **贡献候选**：设计并沉淀了 8 个提示词资产文件，覆盖 9 类任务场景（任务提取/拆解/修改路由/意图分类/当日复盘/角色生图与动作帧/技能创造/技能模板），全部带严格的输出格式约束。
  - 可观察证据：`src/prompts/` 目录下 8 个提示词模板文件，每个都有角色设定、JSON 输出约束、字段校验规则。
  - 简历可用程度：**高**（提示词工程能力的实物证据）

### 项目交付相关
- **贡献候选**：从 0 到 1 交付完整可运行的桌面应用（含安装打包能力），约 2 个月内 19 次功能提交，每次提交对应一个完整功能单元。
  - 可观察证据：Git 历史（2026-04-09 初始化 → 2026-06-05 最近提交；提交信息均为规范的 feat/docs 格式且描述具体功能）；`pnpm tauri build` 可打包桌面应用。
  - 需要补充确认：是否参加了大赛路演/提交？比赛结果？是否有演示视频？
  - 简历可用程度：**高**
- **贡献候选**：交付了 PRD（含演示流程设计和风险应对预案）这一独立产品文档产物。
  - 可观察证据：`cyberpet_prd.docx` 实物，含 5 分钟现场演示脚本（第七节）和风险应对表（第八节）。
  - 简历可用程度：**高**

---

## 8. AI 协作能力证据提取

### 1. 拆解 Decompose
- **PRD 的 EARS 需求拆解**：把「做一个 AI 桌宠」的模糊想法拆解为 6 大功能域、40 条带优先级和触发条件的需求条目，并切分 P0（参赛 demo 必须）/ P1（可延后）/ P2（未来迭代）三层。
- **开发阶段拆解**：Git 历史显示首日按 Phase 1-8 顺序提交（骨架 → 桌宠本体 → 数据层 → LLM 层 → 对话 → 排程 → 复盘 → 设置），是把大项目拆成可串行交付单元的直接证据。
- **产品本身就是「拆解能力」的元证明**：核心功能「任务拆解引擎」要求 AI 把主任务拆为 2-7 个子任务并估时——你为「拆解」这件事本身写了产品规则（子任务数量上限、单项超 120 分钟要再拆，PRD DECOMP-005）。

### 2. 编排 Orchestrate
- **多模型协作链路**：形象工作室串起「LLM 润色提示词 → 生图模型出基准图 → 逐帧生成 → 视频模型生成动作 → 本地抠像」的多模型流水线，每个环节有进度反馈。
- **多窗口产品系统**：5 个独立窗口通过事件总线协同（任务变更广播、桌宠气泡触发、角色切换通知），是「系统编排」而非「页面堆叠」。
- **可复用工作流体系**：技能系统本质是「用户级的 prompt 编排工具」——模板 + 变量注入（用户输入/当前时间/当前任务列表/桌宠名）+ 可选后置动作。

### 3. 验证 Verify
- **AI 输出的多级防错**：LLM 返回的 JSON 有三级容错解析（直接解析 → 剥代码块 → 提取花括号），再经 Zod 模式校验字段合法性（时间格式、优先级范围、类别枚举）——`src/services/taskParser.ts`。
- **AI 生成内容的安全约束**：技能创造器对 AI 生成的技能做字段白名单校验、命名冲突自动去重、禁止生成「创建技能」类技能防止递归——`src/services/skillRegistry.ts`。
- **服务可用性前置检查**：对话/生图/生视频供应商都有「连接检测」，配置缺失时界面提前警告（如「即梦 API Key 还没填，Step 2 无法生图」），而不是让用户撞墙。
- **流程闭环设计**：超时 150% 干预、提醒未响应自动顺延、生成中断草稿恢复、配额超限前熔断——每条主流程都有异常出口。
- **PRD 中的验收标准**：第九节明确定义了技术/体验/叙事三层成功标准（如「拆解结果可用率 ≥ 90%」「观众可感知桌宠理解了我的任务」）。

### 4. 资产化 Build Assets
- **提示词资产库**：`src/prompts/` 下 8 个可复用提示词模板文件，覆盖任务理解、内容生成、图像/视频生成三类场景。
- **供应商配置资产**：VendorPreset 体系把 8 家 AI 供应商的接入方式（端点、默认模型、认证方式、定价、自定义字段）沉淀为结构化配置，新增供应商只需加一份配置。
- **项目记忆文档**：`CLAUDE.md`（架构说明 + 开发注意事项 + 踩坑记录）和 `.claude/PROJECT.md`——为 AI 协作沉淀的「项目长期记忆」，本身就是 AI 协作方法论的资产。
- **产品文档资产**：PRD（含可复用的 EARS 需求模板和演示脚本结构）。

### ⚠️ 代码库证据不足、需要你补充的 AI 协作素材
代码库能证明「结果」，但**协作过程**不可见。建议补充以下素材（按价值排序）：
1. 你与 AI 的典型协作分工：哪些是你判断的（需求、取舍、验收），哪些交给 AI（代码生成、方案初稿）？
2. 一个「AI 给错方案、你如何发现并纠偏」的具体故事（面试杀手锏）。
3. 提示词迭代过程：比如复盘提示词的「三原则」是一次写成还是被坏结果逼出来的？
4. 使用的工具链组合（Claude Code？其他？）和大致的人机时间配比。
5. CLAUDE.md 中「踩坑记录」对应的真实踩坑故事（如透明窗口文字发虚、弹窗被容器劫持等，文档里有线索）。

---

## 9. 可用于 PDF 简历的项目写法

### 版本 A：一行版（约 55 字）
> 独立设计并交付 AI 桌面任务伴侣「赛博桌宠」（AMD AI 大赛参赛作品）：自然语言生成当日计划，AI 拆解、排程、提醒、复盘全闭环。

### 版本 B：三行版
> **赛博桌宠 CyberPet** — AMD AI 智能体大赛参赛作品 · 独立完成（AI 协作开发）
> 针对「待办工具只记录、不帮忙执行」的痛点，用 EARS 方法撰写 40 条结构化需求的 PRD 并界定 MVP，以 AI 协作方式约 2 个月交付可运行桌面应用。
> 产物：可演示的完整产品（对话规划 / 执行提醒 / AI 复盘 / AI 形象工作室）、8 个提示词资产文件、7+ 家 AI 供应商统一接入框架。

### 版本 C：六行版
> **赛博桌宠 CyberPet** — 面向知识工作者的 AI 桌面任务伴侣（AMD 锐龙 AI 智能体创新应用大赛「工作学习」赛道参赛作品）
> **背景**：待办类工具只解决「记录」，任务拆解、排程、执行监督仍靠用户意志力，构成真实弃用原因。
> **角色**：独立承担产品定义、需求拆解、交互设计与 AI 协作开发全流程（Git 历史单作者可验证）。
> **关键动作**：用 EARS 方法产出 40 条带优先级需求的 PRD 并界定 MVP；将核心交互设计为「一句话 → AI 拆解 → 排程 → 到点提醒 → 超时干预 → 当日复盘」闭环；把生图/生视频专业流程封装为 5 步向导；为 AI 复盘制定「具体、诚实、向前」输出约束；设计成本透明 + 配额熔断机制。
> **AI 协作**：立项即把 AI 协作效率纳入选型（PRD 可查）；沉淀 8 个提示词资产文件与项目记忆文档；对 AI 输出建立格式校验、字段白名单、防递归等多级验证。
> **产物**：可运行/可打包的桌面应用、PRD、提示词资产库、约 2 个月 19 次规范化功能提交（仓库可验证）。

---

## 10. 可用于网页版简历的项目页面内容（大纲）

### 项目背景
- AMD 锐龙 AI 智能体创新应用大赛「工作学习」赛道的参赛动机（**需要我补充**：你为何选这个赛道/题目）。
- 洞察：todo 工具的真实空白在「记录之后没人管」——PRD 中的差异化叙事「这不是又一个 todo-list 工具」。

### 目标用户
- 三层用户（知识工作者 / 备考人群 / 自由职业者）+ 各自核心诉求（直接引用 PRD，注明出处增强可信度）。

### 核心问题
- 三层问题分析（见本档案第 2 节），重点讲深层：降低「从想法到行动」的启动成本、把自我管理从意志力问题变成系统问题。

### 我的判断
- 精选 4 个：对话即规划（判断一）、AI 输出可信度约束（判断二）、专业流程向导化（判断三）、成本透明化（判断四）。每个配「问题 → 判断 → 证据」三段式。

### 产品结构
- 7 个模块的功能地图（本档案第 4 节），建议配一张产品结构图。

### 核心页面或功能（建议配截图/录屏）
1. **对话生成计划**（最核心，建议录 GIF：输入一句话 → 任务面板出现日程）
2. **形象工作室 5 步向导**（最惊艳，逐步截图）
3. **足迹复盘页**（AI 复盘文本 + 打卡热力墙）
4. **桌宠气泡提醒**（非打断式交互的截图）

### AI 协作过程
- 可直接展示的证据：PRD 选型表中的「Claude Code 生成效率高」、`src/prompts/` 资产库、CLAUDE.md 项目记忆。
- **需要我补充的问题清单**：见本档案第 8 节末尾的 5 条素材清单。

### 最终产物
- 可运行的桌面应用（可打包安装）/ PRD 文档 / 8 个提示词资产文件 / Git 仓库（19 次规范提交）/ 演示视频（**需要我补充**：是否存在）。

### 复盘反思
- 诚实讲「端侧推理 → 云端 + 本地双模式」的转向（判断七）：PRD 阶段已把它列为最高风险并预设应对方案，最终按预案执行——这反而是「风险预判 → 应对落地」的正面故事。**需要我补充**：转向的具体时间点和决策细节。
- 不足候选（基于代码可观察）：无测试框架、无真实用户验证、复盘的周度趋势等 P2 需求未实现。**需要我补充**：你自己认为的最大遗憾。

---

## 11. 面试官可能追问的问题

1. **「PRD 里写端侧 LLM + NPU 推理，但代码里是调云端 API，为什么？」**
   → 准备：转向的时间点、触发原因（模型质量？集成复杂度？时间盒？）、为何 PRD 第八节提前预设了这个应对方案。这是最尖锐也最值得准备的问题——答好了是「风险管理能力」，答不好是「叙事注水」。
2. **「这个项目里哪些是你判断的，哪些是 AI 生成的？」**
   → 准备：明确的分工边界描述 + 1-2 个「你否决 AI 方案」的具体案例。
3. **「复盘提示词的『三原则』是怎么来的？」**
   → 准备：是先验设计还是被坏案例逼出来的迭代故事（后者更有说服力）。
4. **「形象工作室为什么是 5 步而不是 3 步或者一键生成？」**
   → 准备：每一步存在的理由（候选选择 = 质量控制点；动作清单 = 成本确认点），以及「一键生成」被否决的原因。
5. **「你怎么验证任务拆解的质量？拆解结果不合理怎么办？」**
   → 准备：JSON 校验 + 用户可自然语言重拆（DECOMP-004）+ PRD 中「可用率 ≥ 90%」的验收标准是怎么测的（**需要补充**：实际是否测过）。
6. **「为什么接 7 家供应商而不是只接一家最好的？」**
   → 准备：成本/可用性/用户付费意愿差异的判断；本地通道的兜底价值。
7. **「这个产品的留存抓手是什么？你怎么知道用户会一直用？」**
   → 准备：打卡墙/连续天数的设计意图 + 诚实承认未经过真实用户验证（**需要补充**：是否有任何真实使用反馈）。
8. **「2 个月独立做完这些，最大的瓶颈是什么？」**
   → 准备：具体的踩坑故事（CLAUDE.md 踩坑记录里有线索：透明窗口渲染、跨窗口状态同步等，翻译成产品语言讲）。
9. **「比赛结果怎么样？评委怎么评价的？」**
   → 准备：如实回答（**需要补充**：比赛进展/结果/反馈）。
10. **「如果重做一遍，你会怎么优化？」**
    → 准备：候选答案——先做真实用户验证再扩功能、补充周度趋势分析（PRD P2 未做项）、为拆解质量建立评测集。

---

## 12. 我需要补充的信息清单（按优先级）

| 优先级 | 信息 | 为什么重要 |
|---|---|---|
| 🔴 P0 | 比赛进展与结果（是否提交/路演/获奖/评委反馈） | 直接决定简历能不能写「参赛」之外的结果性表述 |
| 🔴 P0 | 「端侧 → 云端双模式」转向的决策故事 | 面试必问，是档案里唯一的「叙事-代码」差距点 |
| 🔴 P0 | AI 协作分工方式 + 1-2 个纠偏 AI 的具体案例 | AI 协作能力的核心证据，代码库无法体现过程 |
| 🟠 P1 | 演示视频或可下载安装包链接 | 「可验证产物」的最强形态 |
| 🟠 P1 | 项目周期细节（PRD 之前的酝酿期、每周投入时长） | 支撑「项目推进能力」的表述 |
| 🟠 P1 | 提示词迭代过程（尤其复盘三原则、生图绿幕约束的由来） | 提示词工程能力从「有资产」升级为「有方法」 |
| 🟡 P2 | 是否有任何真实用户/同学/评委试用反馈 | 哪怕 3 个人的定性反馈也能显著提升可信度 |
| 🟡 P2 | 踩坑故事 2-3 个（产品语言版本） | 面试细节弹药 |
| 🟡 P2 | 你的角色自述（是否有他人参与美术/测试等） | 确认「独立完成」表述的边界 |
| 🟡 P2 | 你自己的最大遗憾/下一步计划 | 复盘反思节需要 |

---

## 13. 最终项目能力标签

1. **AI 产品设计** — 把 LLM/生图/生视频三类模型能力封装为零门槛产品功能（5 步向导、对话即规划、一键复盘均有代码与界面实证）。
2. **复杂场景抽象** — 把「一天的任务管理」抽象为 计划→任务→子任务→提醒→复盘 的对象模型，把「桌宠陪伴」抽象为状态机驱动的动画体系（数据库结构与状态类型定义可验证）。
3. **需求结构化（EARS）** — 用 EARS 语法产出 40 条带优先级、带触发条件的需求并界定 MVP（PRD 实物可验证）。
4. **提示词工程** — 8 个带格式约束、防编造规则、安全白名单的提示词资产文件（`src/prompts/` 可验证）。
5. **AI 工作流编排** — 设计多模型流水线（润色→生图→生帧→生视频→抠像）和用户级技能编排体系（代码可验证）。
6. **AI 输出质量控制** — 多级 JSON 容错 + 模式校验 + 字段白名单 + 防递归 + 配额熔断的完整验证链路（代码可验证）。
7. **成本与商业敏感度** — 按秒计价透明展示 + 每日配额防护 + 免费本地通道兜底（供应商目录与配额模块可验证）。
8. **Claude Code / AI 协作交付** — 立项即把 AI 协作效率写入技术选型理由（PRD 可验证），并以项目记忆文档沉淀协作上下文（CLAUDE.md / PROJECT.md 可验证）。

---

## 14. 简历素材摘要

> 以下摘要可直接复制到后续简历生成任务中。

**项目名称**：赛博桌宠 CyberPet（AMD 锐龙 AI 智能体创新应用大赛「工作学习」赛道参赛作品）

**一句话定位**：面向知识工作者的 AI 桌面任务伴侣——一句话输入当日待办，AI 完成拆解、排程、到点提醒、超时干预与当日复盘的全闭环；同时把 AI 生图/生视频封装为 5 步向导，让用户零专业知识创建专属桌宠形象。

**目标用户**：知识工作者（主）、备考学生（次）、自由职业者（边缘）——来自 PRD 明确定义。

**核心问题**：待办工具只解决「记录」，拆解/排程/执行监督仍靠用户意志力；同时 AI 能力（提示词、绿幕、多帧一致性）对普通用户门槛过高。

**我的角色候选**：独立产品定义者 + AI 协作开发者（Git 单作者可验证；具体分工细节待本人补充确认）。

**关键产品判断**：① 任务管理重构为「对话即规划」；② 用「具体/诚实/向前」三原则约束 AI 复盘输出可信度；③ 专业 AIGC 流程封装为 5 步可中断恢复向导；④ AI 成本透明化（按秒计价 + 配额熔断 + 免费本地通道）；⑤ 非打断式提醒交互（气泡选择题 + 自动顺延）。

**AI 协作证据**：PRD 技术选型明确写入「Claude Code 生成效率高」；8 个提示词资产文件（含格式约束与防编造规则）；为 AI 助手维护的项目记忆文档（CLAUDE.md / PROJECT.md 含踩坑记录）；对 AI 输出的多级校验体系。协作过程细节待本人补充。

**核心产物**：可运行/可打包的 Tauri 桌面应用（5 窗口完整产品）、含 40 条 EARS 需求的 PRD、8 个提示词资产文件、7+ 家 AI 供应商统一接入框架、约 2 个月 19 次规范化功能提交的 Git 仓库。

**可用于 PDF 简历的一句话**：独立设计并交付 AI 桌面任务伴侣「赛博桌宠」（AMD AI 大赛参赛作品）：自然语言生成当日计划，AI 拆解、排程、提醒、复盘全闭环。

**适合网页版展开的证据**：形象工作室 5 步向导逐步截图、对话生成计划的演示 GIF、复盘页（AI 文本 + 打卡墙）截图、PRD 的 EARS 需求表节选、提示词资产目录截图、Git 提交时间线。

**需要我补充确认的信息**：比赛结果与评委反馈；端侧→云端转向的决策故事；AI 协作分工与纠偏案例；演示视频/安装包链接；真实用户反馈；踩坑故事；他人参与边界。

---

*档案生成日期：2026-06-05 · 证据快照：Git master @ af9fd5e*
